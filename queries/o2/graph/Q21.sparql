# Q21: Electrical Resilience (SPOF Detection)
# Status: IMPOSSIBLE pour O2
# Raison: SPARQL ne supporte pas allShortestPaths ou enumeration de tous les chemins
PREFIX btb: <http://basetype.benchmark/ontology#>

SELECT ("IMPOSSIBLE: O2 ne supporte pas allShortestPaths" AS ?error)
WHERE {}

# Alternative tres degradee : lister les noeuds sur UN chemin
# Mais cela ne permet pas de detecter les SPOF
